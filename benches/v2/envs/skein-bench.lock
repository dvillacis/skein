# skein-glm benchmark Python environment.
#
# Regenerate with:
#   uv pip compile pyproject.toml --extra bench --output-file benches/v2/envs/skein-bench.lock
#
# Versions are pinned at Phase A scaffolding time and refreshed when
# the headline figures are regenerated for a release.

# --- benchmark harness ---
snakemake==8.*
pyarrow>=15
pyyaml>=6

# --- skein + comparators ---
# skein-glm           # installed editable: pip install -e .[bench]
scikit-learn>=1.4
skglm>=0.3
celer>=0.7
statsmodels>=0.14
lifelines>=0.27

# --- data / plotting ---
numpy>=1.26
scipy>=1.11
pandas>=2.1
matplotlib>=3.8
seaborn>=0.13
jupytext>=1.16
